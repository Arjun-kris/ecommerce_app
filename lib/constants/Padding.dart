
// ignore_for_file: file_names

import 'package:flutter/material.dart';

const textFieldContentPadding = EdgeInsets.all(15.0);
const profilecontain = EdgeInsets.symmetric(horizontal: 16.0);
const p1 = EdgeInsets.symmetric(horizontal: 5.0);
const top8 = EdgeInsets.only(top: 8.0);
const paddingall8 = EdgeInsets.all(8.0);
const paddingall10 = EdgeInsets.all(10.0);
const paddingall16 = EdgeInsets.all(16.0);
